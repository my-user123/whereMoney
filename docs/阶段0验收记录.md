# 阶段 0 验收记录

## 1. 验收范围

本记录对应 `开发任务计划.md` 中：

1. 实施前需要锁定的问题。
2. 阶段 0：项目骨架与接口契约收敛。

说明：当前工作区已存在提前完成的 Phase 1 代码，本次验收不回退已有代码，只补齐 Phase 0 所要求的骨架、契约和验证记录。

## 2. 实施前问题锁定结论

| 问题 | Phase 0 结论 |
| --- | --- |
| 账户币种与账单币种 | v1 强制一致，前端选择账户后带出币种，后端最终校验 |
| AI 报告内容格式 | v1 使用 Markdown 字符串，`riskNotice` 单独字段 |
| Dashboard、图表、周期提醒实例、通知、导出记录响应结构 | 已在 `docs/API设计文档.md` 补齐草案 |
| 详情 API | 已在 `docs/API设计文档.md` 补齐账户、分类、预算、储蓄目标、周期提醒详情路径 |
| ResponseCode | 已锁定业务细分码方向，按实现阶段逐步补齐 |
| 账单币种筛选索引 | 已包含 `transactions(user_id, currency, occurred_at)` |
| 导出任务百分比 | v1 不显示百分比，仅展示任务状态 |
| 默认初始化策略 | 注册后初始化默认分类；不自动创建默认账户 |

## 3. 阶段 0 骨架检查

| 领域 | 当前状态 |
| --- | --- |
| Backend | Spring Boot 3 工程已建立，包含统一响应、全局异常、JWT、当前用户上下文、MyBatis-Plus、XML mapper 目录 |
| Backend 模块 | `auth`、`user`、`account`、`category`、`transaction`、`analytics` 已有实现，`budget`、`saving`、`recurring`、`ai`、`notification`、`report` 已补占位 |
| Frontend | Next.js App Router、Tailwind、AppShell、底部导航、桌面侧边导航、API client、状态组件、design tokens 已建立 |
| Database | Flyway migration 已建立，覆盖用户、资料、账户、分类、账单基础表和关键索引 |
| Nginx/Docker | Compose 包含 PostgreSQL、Redis、RabbitMQ、backend、frontend、nginx；Nginx 已配置 `/`、`/api/` 和预留 `/ws/` |
| WebSocket | v1 暂不启用，仅在 Nginx 和架构文档预留 `/ws/` |

## 4. 验证命令

已执行或本阶段需要执行的验证：

```powershell
.\scripts\format-java.ps1 .\backend\src\main\java .\backend\src\test\java
if (Test-Path .\backend\mvnw.cmd) { .\backend\mvnw.cmd test } else { mvn -f .\backend\pom.xml test }
npm run build
docker compose config
git diff --check
```

## 5. 已知说明

1. `docker-compose.yml` 仍保留模板中的 MySQL 服务；本项目 v1 使用 PostgreSQL，MySQL 不参与当前实现。
2. 真实 PostgreSQL 容器启动和生产 migration 验证可在后续本地联调时执行。
3. Phase 0 不实现预算、储蓄目标、周期提醒、AI、通知和导出业务逻辑。
